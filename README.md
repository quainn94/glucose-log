# glucose-log v0.3.7 — GitHub Pages

아빠가 휴대폰에서 간단히 쓰는 1인용 혈당 기록 웹앱입니다.

## 기능
- Supabase 이메일/비밀번호 로그인 및 로그인 유지
- 월간 달력형 혈당 기록
- 날짜 클릭 → 아침 식전 / 저녁 식전 혈당 입력 및 수정
- 아침 식전 목표 80~120 mg/dL
- 저녁 식전 목표 100~150 mg/dL
- 앱 화면에서 목표 범위 내 초록 / 범위 밖 빨강 / 미측정 회색
- 현재 보고 있는 월을 `.xlsx`로 내보내기
- Android에서 지원 시 공유/앱 선택 화면으로 Excel 또는 Google Sheets에 바로 전달

## v0.2.8.6 엑셀 변경
- 사용자가 제시한 월간 혈당표 형태로 변경
- `구분 | 월 | 화 | 수 | 목 | 금 | 토 | 일` 8열 구성
- 주차마다 `날짜 / 아침 / 저녁` 3행 구조
- 날짜행은 연보라색, 아침행은 연노랑, 저녁행은 흰색
- 판정 문구 없이 수치만 표시
- 목표 범위는 초록 글씨, 범위 밖은 빨간 글씨
- 한 달 전체가 한 화면/한 페이지에 들어오도록 구성

## GitHub Desktop 업데이트
압축을 풀고 기존 `glucose-log` 저장소 폴더에 파일을 덮어쓴 뒤:
1. Summary에 `v0.3.7` 입력
2. `Commit to main`
3. `Push origin`

GitHub Pages가 자동으로 최신 버전을 배포합니다.


## v0.2.8.6
- 엑셀 내보내기 레이아웃을 사용자 예시와 동일한 방향으로 확대 조정
- 주차 사이 간격행 추가
- 월 밖의 빈 날짜 구역은 큰 흰 영역으로 병합해 내부 세로선 제거
- 제목/목표범위/요일/혈당 글자와 행·열 크기 확대


## v0.2.8 엑셀 서식 변경
- 주차 사이 병합 간격행: 10px(ExcelJS 7.5pt)
- 일자/아침/저녁 및 혈당 수치: 15pt
- 월간표 가장 바깥 테두리: thick
- 각 주차 블록 외곽 테두리: medium
- 내부 셀 테두리: thin


## v0.2.8
- 기존 기록이 있는 날짜에는 `이 날짜 기록 삭제` 버튼이 표시됩니다.
- 삭제 전 확인창이 표시되며, 해당 날짜의 아침/저녁 기록을 함께 삭제합니다.
- 한쪽 값만 지우고 싶으면 해당 입력칸만 비운 뒤 저장하면 됩니다.

## v0.2.9
- 홈 화면/브라우저 아이콘을 연붉은 배경 + 흰 물방울/의료 십자가 아이콘으로 변경
- Android 홈 화면 추가용 Web App Manifest 추가


## v0.3.7
- 기준 버전: v0.2.9 (별도 PWA/service worker 대응은 포함하지 않음)
- 월 상단 제목을 눌러 연도/월 빠른 선택 창을 열 수 있음
- 선택 창에서 연도를 좌우 한 번씩 이동하고 12개월 중 원하는 월을 바로 선택
- 예: 2026년 8월 → 2027년 원하는 월을 2번 조작으로 이동 가능
- 엑셀 A4 출력 시 좌우 중앙 정렬 유지
- A4 상단 여백을 약 0.85 inch(약 21.6mm)로 늘려 호치키스 공간 확보
- 세로 중앙 정렬은 사용하지 않아 표가 상단 여백 아래부터 시작


## v0.3.7 인쇄 보정
- 실제 A4 출력 기준 표가 오른쪽으로 치우쳐 보이는 현상을 보정하기 위해 왼쪽 여백을 줄이고 오른쪽 여백을 늘려 약간 왼쪽으로 이동했습니다.
- 상단 여백을 기존보다 약 1cm 추가해 호치키스 공간을 넉넉하게 확보했습니다.
- A4 세로 1페이지 맞춤, 가로 중앙 배치는 유지합니다.


## v0.3.7
- 앱 월간 달력과 엑셀 내보내기 모두 주간 시작을 일요일로 변경했습니다.


## v0.3.7 인쇄 보정
- 엑셀 출력표를 A열부터 시작하도록 변경해 빈 여백용 열 제거
- A4 인쇄 좌우 여백을 각각 0.9cm로 통일
- 좌우 중앙 정렬은 페이지 설정으로 처리
- 기존 상단 호치키스 여백, 일요일 시작 주간, 월/연도 선택 기능 유지


## v0.3.7 변경
- 엑셀 인쇄 위치를 기존보다 왼쪽으로 약 1mm 보정 (좌 0.9cm / 우 1.1cm 여백, 가로 중앙 정렬)
- 엑셀 날짜행 배경을 옅은 회색 계열로 변경
- 목표 하한 미만 혈당은 보라색 표시: 아침 <80 / 저녁 <100
- 목표 범위는 초록, 목표 상한 초과는 빨강 유지
- 앱/엑셀에서 일요일 날짜를 빨간색 표시
- 대한민국 공휴일 날짜와 공휴일 명칭을 앱/엑셀에 표시
- 공휴일 정보는 월력요항 기반 공개 데이터(`hyunbinseo/holidays-kr`)를 런타임에 불러오며, 네트워크 오류 시 2026년 기본 공휴일 데이터를 사용


## v0.3.7
- 앱 화면에 현재 버전과 간단한 업데이트 내용을 표시
- 대체공휴일을 `광복절대체`, `석탄일대체`처럼 축약 표시
- 공휴일명 8pt
- 엑셀 날짜행 높이 30px 상당(22.5pt) 유지
- 일요일/공휴일 빨강을 `#cc0000`으로 통일


## v0.3.7
- 2027년 이후 공휴일 데이터가 표시되지 않던 파싱 오류 수정
- 외부 공휴일 JSON의 연도별 중첩 구조 지원
- `기독탄신일` 표시를 `성탄절`로 변경


## v0.3.7
- 앱의 업데이트 영역을 `최신 업데이트`로 변경
- 최근 반영 날짜/시간 표시: `2026-08-19 00:42`
- 최근 3개 버전(v0.3.6 / v0.3.5 / v0.3.4) 수정 내역을 간단히 확인 가능
